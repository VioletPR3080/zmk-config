# USB 1000Hz polling (1ms interval)
CONFIG_USB_HID_POLL_INTERVAL_MS=1

# BLE low-latency connection interval (7.5ms - 10ms)
CONFIG_BT_GAP_PERIPHERAL_PREF_MIN_INT=6
CONFIG_BT_GAP_PERIPHERAL_PREF_MAX_INT=8

# Enable Bluetooth
CONFIG_ZMK_BLE=y
